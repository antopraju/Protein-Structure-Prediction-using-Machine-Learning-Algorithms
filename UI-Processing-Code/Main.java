package io.code;

//Main class
public class Main {
	
	public static void main(String[] args) throws Exception {
		@SuppressWarnings("unused")
		Maps maps=new Maps();
		
		//Dependency Injection - injecting maps class through constructor to objects of other classes
		//Composition comp=new Composition(maps);
		//Transition trans=new Transition(maps);
		//PercentFrequency percent=new PercentFrequency(maps);
		//Distribution distro=new Distribution(comp);
		//@SuppressWarnings("unused")
		//VectorForm vector=new VectorForm(maps,comp,trans,distro,percent);
	}
}
